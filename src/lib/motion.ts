/**
 * Shared motion tokens. The easing is typed as a 4-number tuple so
 * framer-motion accepts it as a cubic-bezier (a bare array infers as number[]).
 */
export const EASE_OUT_EXPO: [number, number, number, number] = [0.16, 1, 0.3, 1];
